# -*- coding: utf-8 -*-
"""시스템 설정. 추천 개수·유니버스·팩터 가중치를 여기서 조절한다."""

# ── 추천 개수 ────────────────────────────────────────────
KR_PICKS = 10          # 한국 추천 종목 수
US_PICKS = 10          # 미국 추천 종목 수

# ── 유니버스(후보 풀) 크기 ───────────────────────────────
KR_UNIVERSE_SIZE = 200       # 코스피+코스닥 시총 상위 N개
KR_MIN_MARCAP = 5e11         # 최소 시총 5천억원 (소형주 제외)
US_MIN_MARCAP = 2e9          # 최소 시총 20억달러
# 미국은 S&P500 전체를 유니버스로 사용

# ── 멀티팩터 가중치 (합 = 1.0) ───────────────────────────
#   가치·우량(value+quality) 0.40 / 성장 0.30 / 모멘텀 0.30
FACTOR_WEIGHTS = {
    "value":    0.25,   # forwardPE·PSR·PEG (낮을수록 좋음)
    "quality":  0.15,   # ROE·이익률·부채비율
    "growth":   0.30,   # 매출·이익 성장률
    "momentum": 0.30,   # 3·6개월 수익률·52주고가 근접도
}

# ── 캐시 ────────────────────────────────────────────────
CACHE_DIR = "cache"
CACHE_HOURS = 12       # 이 시간 안의 캐시는 재사용 (yfinance 호출 절감)

# ── 테마 부스트 ─────────────────────────────────────────
# 메가테마(AI인프라·방산·조선 등) 매칭 시 score_total 가산.
# 종합 추천에서 효성중공업 같은 구조적 수혜주를 부각시키는 용도.
THEME_BOOST_PER_HIT = 0.04   # 매칭된 테마 하나당 가산
THEME_BOOST_MAX = 0.10       # 가산 상한 (테마 N개 매칭돼도 이만큼만)

# ── 성장 단일축 추천 개수 ───────────────────────────────
KR_GROWTH_PICKS = 10
US_GROWTH_PICKS = 10

# ── 동반강세 산업 발굴 ──────────────────────────────────
HOT_THEME_MIN_STOCKS = 3   # 한 산업 내 최소 종목 수
HOT_THEME_TOP_N = 5        # 발굴할 상위 산업 수

# ── 데이터 수집 throttle ─────────────────────────────────
YF_SLEEP = 0.25        # yfinance .info 호출 간 대기(초)
YF_RETRY = 2           # 실패 시 재시도 횟수
